services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: app
      DATABASE_URL: postgres://app@postgres/app
  redis:
    image: redis:7
    environment:
      - REDIS_URL=redis://redis
