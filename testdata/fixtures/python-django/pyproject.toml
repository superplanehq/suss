[project]
name = "widget"
requires-python = ">=3.12"
dependencies = [
    "django>=5.0",
    "ruff",
]

[project.optional-dependencies]
dev = ["pytest", "pytest-django"]

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.ruff]
line-length = 88
